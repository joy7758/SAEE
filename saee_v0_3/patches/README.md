# v0.3 Patch Surface

This directory records the self-correction pass for SAEE v0.3.

The current pass generated additive modules under `saee_v0_3/` instead of
rewriting v0.1 or v0.2. This preserves backward compatibility and truth
surface separation.

