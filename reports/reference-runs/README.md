# Local Reference Runs

This directory contains agent-readable records of bounded local uses of existing SAEE capabilities by other projects. A reference run is evidence about a specific local invocation. It is not customer validation, production adoption, external interoperability, certification, deployment authorization, or a capability-inventory state change.

Each record must preserve the source repository revision, request boundary, observed output, missing evidence, side-effect boundary, and non-claims.
