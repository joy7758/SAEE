\section{Experiments and Simulation Protocol}

The experimental material is not newly generated here. This section
restructures existing local SAEE observations into ALife-style simulation
language.

The frozen evidence chain is:
\[
\begin{aligned}
&\text{v1.0 runtime}
\rightarrow \text{long-horizon experiment}
\rightarrow \text{behavior / phase analysis}\\
&\rightarrow \text{phase diagram}
\rightarrow \text{invariant clusters}
\rightarrow \text{candidate law set}.
\end{aligned}
\]

The stable runtime is a local deterministic evolutionary loop. The
long-horizon experiment observes the runtime without feeding analysis back
into the kernel. The phase-space compression derives regime, attractor, and
invariant surfaces from existing logs only. The candidate law-set artifact is
reported paper-facing as five falsifiable candidate regularities from the
frozen phase-space object.

Key local simulation facts used in this paper are:
\begin{itemize}
  \item 100-generation long-horizon observation surface.
  \item Dominant regime: \texttt{stable\_regime}.
  \item Dominant attractor basin: \texttt{stable\_lineage\_basin}.
  \item Only observed transition: \texttt{stable\_regime -> stable\_regime}.
  \item Collapse observed: \texttt{false}.
  \item Cross-regime transition observed: \texttt{false}.
  \item Lineage graph surface: 808 nodes and 1590 edges in the long-horizon
    lineage report.
  \item Candidate law count: 5.
  \item External validated law count: 0.
\end{itemize}

No new simulation is performed for this ALife-format package.

\paragraph{Reported run specification.}
This manuscript characterizes the frozen SAEE configuration already present in
the repository and does not introduce additional runs here. For
reproducibility, the repository materials are the authoritative source for the
entry point, configuration, seed, horizon, and figure-generation pipeline that
produced the reported statistics. The 808-node / 1590-edge counts are read as
raw long-horizon lineage graph totals. The semantic-drift maximum of 0.2 is
read from the existing Phase II cross-generation drift surface, not introduced
as a new experiment in this manuscript.
