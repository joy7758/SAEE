# MCP Client Example

使用 `example_config.json` 启动仓库内 stdio Adapter。Agent 通过 `tools/list` 找到 `evaluate_rehearsal_run`，随后提交严格的本地合成 Run。结果中的 `SUPPORTED` 只代表固定剖面要求满足，不代表安全、认证、授权或部署批准。

本示例不连接外部 MCP Server。

