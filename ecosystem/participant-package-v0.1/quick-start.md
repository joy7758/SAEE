# Future evaluator quick start

1. 确认已获得独立外部验证授权；当前仓库没有该授权。
2. 从 Alpha Manifest 核对两项能力和三项操作。
3. 选择 `mcp_stdio_local`、`http_localhost`、`generic_adapter_pattern` 或 `documentation_only`。
4. 运行五个测试维度，不使用生产数据。
5. 只填写结构化反馈字段，不附原始 prompt、凭据或私有日志。
6. 将结果解释为兼容性观察，不解释为采用、认证或生产可靠性。
