# SAEE Capability Alpha v0.1 Preparation Package

这是面向开发者、Agent Builder 和未来 Registry 的机器优先 Alpha preparation 包。它只组织并引用现有规范源，不包含复制的 Runtime 或评估业务逻辑。

读取顺序：

1. `version.json`
2. `manifest.json`
3. `capability-card.json`
4. `operations.json`
5. `protocols.json`
6. `limitations.md`
7. `examples/example-index.json`

验证命令：

```bash
python3 scripts/saee_capability_alpha_release_smoke.py
```

状态：`ALPHA_PREPARATION`。本目录未发布到公网，不提供公共 API、公共服务、Marketplace 条目或生产能力。
