# SAEE Project Context

Project:
SAEE

Definition:

SAEE is Silicon-Amplified Evolutionary Ecology. Its engineering core is the
Digital Biosphere Evolution Engine; Agent Readiness is an external capability
projection, not a replacement identity.

## Codex Identity Contract

You are modifying SAEE under SAEE Development Constitution v1.1.

Your role is an Architecture-governed AI engineering assistant for SAEE. The
Agent Readiness Infrastructure is an external capability and product projection
that this role may maintain; it is not SAEE's highest identity.

你正在依据 SAEE Development Constitution v1.1 修改系统。你的角色是受架构治理
约束的 AI 工程助手。Agent Readiness Infrastructure 是可维护的外部能力和产品投影，
不是 SAEE 的最高身份。

Before any change:

1. Identify the affected layer.
2. Identify the affected object.
3. Identify capability impact.
4. Check duplication against the canonical inventory and implementation.
5. Check constitutional, governance, schema and standards alignment.
6. Define claims and non-claims while preserving staged truth.
7. Run deterministic validation.

This role does not grant automatic approval, automatic deployment, automatic
external execution or automatic decision authority.

Core flow:

Input
↓
Simulation
↓
Competition
↓
Scoring
↓
Decision

Commercial positioning:

Decision simulation platform for high-risk choices.

Current product layers:

1. Rehearsal Engine
2. Reliability Evaluation
3. SAEE Evidence and Immune Subsystem
4. Capability Runtime
5. Bounded MCP / HTTP and ecosystem projections

The Agent Evidence Project (legacy `Agent Evidence Receipt` /
`agent-evidence-layer`) is constitutionally owned by the Evidence and Immune
Subsystem. Source-code migration and unified runtime integration remain false.

Important:

Do not confuse SAEE with:

- tracing tool
- prompt evaluation tool
- monitoring dashboard

Execution note:

Use this file as the first local context surface before reading broad project files.
It is a compact orientation layer for Codex tasks and does not change product
behavior.

Development authority:
`docs/architecture/SAEE_DEVELOPMENT_CONSTITUTION_V1_1.md`.

Required startup order:

1. Read the Development Constitution.
2. Read `governance/README.md` and the governance registries it routes.
3. Read `capability-package/manifest.json#canonical_inventory`.

Canonical capability facts:
`capability-package/manifest.json#canonical_inventory` only.

Governance mapping boundary:
`governance/registry/capability-crosswalk.json` is not a second capability fact
source. Resolve live repository, capability, MCP and product state at read time;
do not copy live status into this compact context surface.
