# SAEE Change Policy

Small change first.

Do not combine these into one task unless explicitly instructed:

- architecture change
- documentation change
- UI change
- backend change

One task:

One objective.

Keep changes narrow and file-backed. If a task needs product behavior changes,
create or locate the relevant recommendation gate before implementation.

Do not treat approval as execution. Do not treat a local validator pass as
blocker closure.
